let storage = {};

const getItem = key => {
  return storage[key];
};

const setItem = (key, value) => {
  storage[key] = value;
};

const removeItem = key => {
  return delete storage[key];
};

const clear = () => {
  storage = {};
  return true;
};

export const cache = {
  getItem,
  setItem,
  removeItem,
  clear
};
//# sourceMappingURL=cache.js.map